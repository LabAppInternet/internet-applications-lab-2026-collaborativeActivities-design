package cat.tecnocampus.nplusone;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class NPlusOneApplicationTests {

    @Test
    void contextLoads() {
        // Verifies the whole application wires up and the CommandLineRunner runs
        // (seeding + the three reads) without error.
    }
}
