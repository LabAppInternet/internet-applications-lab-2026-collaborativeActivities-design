package cat.tecnocampus.projections;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ProjectionsApplicationTests {

    @Test
    void contextLoads() {
        // Wires the app and runs the seeding + the three reads without error.
    }
}
